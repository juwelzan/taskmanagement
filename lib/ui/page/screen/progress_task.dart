import '../../../core/path/path.dart';

class ProgressTask extends StatelessWidget {
  const ProgressTask({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("ProgressTask")));
  }
}
