import '../../../core/path/path.dart';

class CanceledTask extends StatelessWidget {
  const CanceledTask({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("CanceledTask")));
  }
}
